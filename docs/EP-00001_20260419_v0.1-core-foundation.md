# EP-00001 — v0.1 Core Foundation

## Problem / Pain Points
- No project structure exists yet — need scaffolding before any feature work
- Core library (storage, file ops, search) must be in place before MCP server or CLI can be built
- Need a working MCP server with basic tools to validate the architecture end-to-end

## Suggested Solution
Build the minimum viable foundation: project scaffolding, core library, MCP server with basic tools, and a minimal CLI. No review pipeline, promotion, or advanced features — those are v0.2+.

## Implementation Phases

### Phase 1 — Project Scaffolding
- [ ] Create `pyproject.toml` with dependencies (`mcp`, `duckdb`, `dbmate-bin`, `pytest`, `pyright`)
- [ ] Set up package structure: `src/agent_knowledge/`
- [ ] Create `.env.template`
- [ ] Set up `uv` virtual env and verify deps install
- [ ] Create dbmate migrations for SQLite schema (projects, sessions, turns, memory_edits)
- [ ] Verify: `uv run dbmate up` runs clean

### Phase 2 — Core Library
- [ ] `src/agent_knowledge/core/config.py` — load config from pyproject.toml
- [ ] `src/agent_knowledge/core/storage.py` — SQLite operations (CRUD for projects, sessions, turns, memory_edits)
- [ ] `src/agent_knowledge/core/memory.py` — file operations for `/memory` folder (create, read, update, delete markdown pages)
- [ ] `src/agent_knowledge/core/search.py` — DuckDB persistent search index (BM25 on knowledge + skills, synced from markdown files on every server startup)
- [ ] Write tests for storage, memory, and search modules
- [ ] Verify: all tests pass

### Phase 3 — MCP Server
- [ ] `src/agent_knowledge/server.py` — MCP server entry point
- [ ] Wire core library as MCP tools:
  - [ ] `project_create`, `project_list`
  - [ ] `session_start`, `session_end`, `session_log`
  - [ ] `memory_search`, `memory_read`, `memory_index`, `memory_history`
  - [ ] `memory_create`, `memory_update`, `memory_delete`
  - [ ] `maintain_reindex` (rebuilds in-memory index on demand)
- [ ] Tool descriptions with behavioral guides (curated knowledge is priority)
- [ ] Verify: MCP server starts, tools are discoverable by a client

### Phase 4 — Minimal CLI
- [ ] `src/agent_knowledge/cli.py` — CLI entry point using the core library directly
- [ ] `akw init` — create data directory, folder structure, run migrations
- [ ] `akw status` — show system stats
- [ ] Register `akw` as a script entry point in `pyproject.toml`
- [ ] Verify: `akw init` creates expected folder structure, `akw status` runs

## Out of Scope (v0.2+)
- Review pipeline (`review_get_pending`, `review_complete`, session/daily review)
- Promotion tools (`promote_to_knowledge`, `promote_to_skill`)
- `session_start` context loading (recommended skills/knowledge)
- MCP prompts (`session-bootstrap`, `session-wrapup`)
- Secret scanning (write-time content sanitization)
- Orphan detection and auto-close
- `maintain_get_stats`, `maintain_purge`
- CLI `akw review`, `akw purge`, `akw reindex`, `akw sessions`, `akw search`

## Status: IN PROGRESS
